# Writes a program which takes input p,t,r compute the simple interest & display the result
p = int(input("Enter the p: "))
r = int(input("Enter the r: "))
t = int(input("Enter the t: "))
si = (p*t*r)/100
print(si)